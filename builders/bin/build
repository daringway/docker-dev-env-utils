#!/usr/bin/env bash

APP=$1
VERSION=$2
ARCH=$(uname -m)

TS_FILE=/tmp/start_ts

touch $TS_FILE
eval build-${APP} ${VERSION}
(cd /usr/local; find . -newer $TS_FILE | tar czvf /dist/${APP}-${ARCH}-${VERSION}.tar.gz -T -) 